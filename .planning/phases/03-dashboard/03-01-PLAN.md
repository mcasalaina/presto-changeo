---
phase: 03-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/Dashboard.tsx
  - frontend/src/components/MetricsPanel.tsx
  - frontend/src/components/VisualizationArea.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
autonomous: true

must_haves:
  truths:
    - "User sees metrics panel with placeholder KPIs"
    - "User sees central visualization area"
    - "Dashboard renders without errors"
  artifacts:
    - path: "frontend/src/components/Dashboard.tsx"
      provides: "Main dashboard container layout"
      min_lines: 30
    - path: "frontend/src/components/MetricsPanel.tsx"
      provides: "Metrics and KPIs display"
      min_lines: 40
    - path: "frontend/src/components/VisualizationArea.tsx"
      provides: "Central visualization container"
      min_lines: 25
  key_links:
    - from: "App.tsx"
      to: "Dashboard.tsx"
      via: "import and render in main panel"
      pattern: "import.*Dashboard"
    - from: "Dashboard.tsx"
      to: "MetricsPanel.tsx"
      via: "import and render"
      pattern: "import.*MetricsPanel"
---

<objective>
Create dashboard layout with metrics panel and visualization area.

Purpose: Build the visual structure for the central dashboard panel that will display industry-appropriate metrics and AI-generated visualizations.
Output: Working dashboard components with placeholder data, styled to match the layout prototype.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Layout reference
@layout_prototype.png

# Current frontend structure
@frontend/src/App.tsx
@frontend/src/App.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MetricsPanel component</name>
  <files>frontend/src/components/MetricsPanel.tsx</files>
  <action>
Create a MetricsPanel component displaying placeholder metrics in a grid layout.

Based on the layout prototype, include:
- Location metric (e.g., "Location: 12.5 Million Km")
- Temperature metric (e.g., "-66.34 °C")
- Angular velocity/Speed metric
- CPU/Load metric
- Use a 2-column grid layout
- Each metric: label, value, unit

Props interface:
```typescript
interface MetricsPanelProps {
  metrics?: {
    label: string
    value: string | number
    unit?: string
  }[]
}
```

Use default placeholder metrics if none provided. Style with dark theme to match prototype (dark background, light text, subtle borders).

Do NOT use any external charting library - just styled divs and text.
  </action>
  <verify>npm run build in frontend directory succeeds</verify>
  <done>MetricsPanel component renders placeholder metrics in grid layout</done>
</task>

<task type="auto">
  <name>Task 2: Create VisualizationArea component</name>
  <files>frontend/src/components/VisualizationArea.tsx</files>
  <action>
Create a VisualizationArea component as the central container for AI-generated visualizations.

Based on the layout prototype (right side shows orbital diagram):
- Large central area for main visualization
- Placeholder state showing welcome message (reuse existing viz-content pattern)
- Accept `content` prop that can be React node or null
- When content is null, show placeholder
- When content provided, render it

Props interface:
```typescript
interface VisualizationAreaProps {
  content?: React.ReactNode
  title?: string
}
```

Style: Dark background, rounded corners, centered content, generous padding.
  </action>
  <verify>npm run build in frontend directory succeeds</verify>
  <done>VisualizationArea component renders with placeholder or provided content</done>
</task>

<task type="auto">
  <name>Task 3: Create Dashboard container and integrate into App</name>
  <files>frontend/src/components/Dashboard.tsx, frontend/src/App.tsx, frontend/src/App.css</files>
  <action>
Create Dashboard.tsx as container component:
- Import and compose MetricsPanel and VisualizationArea
- Layout: metrics on left/top, visualization in center (flexbox)
- Accept props for metrics and visualization content

Props interface:
```typescript
interface DashboardProps {
  metrics?: MetricsPanelProps['metrics']
  visualization?: React.ReactNode
}
```

Update App.tsx:
- Import Dashboard component
- Replace the current `.visualization-area` div with Dashboard component
- Pass placeholder metrics and null visualization initially

Update App.css:
- Add styles for dashboard layout (`.dashboard-container`, `.dashboard-metrics`, `.dashboard-visualization`)
- Use dark theme colors matching existing chat panel
- Ensure responsive behavior (metrics stack on narrow screens)

IMPORTANT: Keep the existing bottom tabs in App.tsx - Dashboard is the content area ABOVE the tabs.
  </action>
  <verify>npm run build succeeds; npm run dev shows dashboard with metrics panel and visualization area</verify>
  <done>Dashboard renders in App with MetricsPanel and VisualizationArea; tabs still work</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` in frontend directory succeeds without errors
- [ ] Dashboard component renders MetricsPanel and VisualizationArea
- [ ] Metrics display in a grid layout with placeholder values
- [ ] Visualization area shows placeholder content
- [ ] Bottom tabs still visible and clickable
- [ ] No TypeScript errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Dashboard matches layout prototype structure
- Components are reusable with props
  </success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard/03-01-SUMMARY.md`
</output>
