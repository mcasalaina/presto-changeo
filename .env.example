# Presto-Change-O Environment Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# Azure AI Foundry Configuration
# =============================================================================

# Azure AI Foundry project endpoint
# Found in Azure AI Foundry portal under your project settings
AZURE_PROJECT_ENDPOINT=https://aipmaker-project-resource.services.ai.azure.com/api/projects/aipmaker-project

# Default model deployment name for chat completions
# Available deployments: model-router, gpt-image-1-mini, gpt-realtime
AZURE_DEPLOYMENT_NAME=model-router

# =============================================================================
# Optional Configuration
# =============================================================================

# Logging level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# =============================================================================
# Notes
# =============================================================================
# - Authentication uses InteractiveBrowserCredential (opens browser for login)
# - Never commit the actual .env file with credentials
# - The .env file is ignored by git (see .gitignore)
